rootProject.name = "esoco-gwt"

['esoco-common', 'esoco-monads', 'objectrelations', 'esoco-lib', 
'esoco-storage', 'esoco-business', 'gewt'].each {
    project -> if (new File("$rootDir/../$project").exists()) includeBuild "../$project"
}